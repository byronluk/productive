// @flow

export type days = {
  Su: boolean,
  M: boolean,
  T: boolean,
  W: boolean,
  Th: boolean,
  F: boolean,
  S: boolean
};

export type daysClass = {
  Su: string,
  M: string,
  T: string,
  W: string,
  Th: string,
  F: string,
  S: string
};

export type weekly = {
  times: number,
  biWeekly: boolean
};
